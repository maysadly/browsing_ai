version: "3.9"

services:
  agent-api:
    build: .
    command: uvicorn agent.server.app:app --host 0.0.0.0 --port 8000
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./runs:/app/runs
      - ./logs:/app/logs
    shm_size: "2gb"
